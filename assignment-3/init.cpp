#include "init.h"
#include "assignment3.h"

/**
 * Don't put anything in this file except for testing purposes 
 * because I will overwrite it when grading your assignment.
 */

/**
 * This function sets the initial IAT if any
 */
void init ( void )
{
	// example code to create a Sierpinski Gasket
/*
	vector<Matrix> iat;
	iat.push_back ( scale ( Pt ( -.9, -.9 ), 0.5 ) );
	iat.push_back ( scale ( Pt ( .9, -.9 ), 0.5 ) );
	iat.push_back ( scale ( Pt ( 0, .56 ), 0.5 ) );

	setIATTransformations ( iat );
*/
}
